# CLAUDE.md — TABI Project

Claude Code context file. Loaded at session start.

---

## Project Overview

**TABI** — English-language curation site introducing Japan to the world.  
Static HTML site. All pages generated by PowerShell scripts.

- Repository: `github.com/Raynart/TABI`
- Production domain: `tabi.guide` (domain pending)
- Hosting: **GitHub Pages** via `.github/workflows/deploy.yml`, auto-deploys on push to `master`
- Language: **English** (all content and UI)

---

## Separation from World Picks

| | World Picks | TABI |
|---|---|---|
| Repo | `New-project` | `TABI` |
| Target | Japanese readers | International audience |
| Language | Japanese | English |
| Design | Tech / high-density | Wabi-sabi minimal |

**These are completely separate projects. Never mix code or content between them.**

---

## Categories & Tags

Both lists live in `site.config.json` and are **enforced** — `generate-pages.ps1`
aborts the build if an article uses a category or tag that is not configured.

### Categories
| Slug | Label |
|------|-------|
| `before-you-go` | Before You Go |
| `rules-etiquette` | Rules & Etiquette |
| `getting-around` | Getting Around |
| `eat-drink` | Eat & Drink |
| `things-to-buy` | Things to Buy |
| `hidden-japan` | Hidden Japan |

### Tags (fixed taxonomy — use only these)
`accommodation` / `affiliate` / `budget` / `festival` / `first-time` / `food` / `kyoto` / `language` / `luxury` / `nature` / `off-the-beaten-path` / `osaka` / `practical` / `shopping` / `tokyo` / `tradition` / `transport`

---

## Key Files

| File | Role |
|------|------|
| `site.config.json` | Site-wide config (URL, GA4, categories, tags) |
| `articles.json` | All article data (Codex scope) |
| `styles.css` | Design system |
| `script.js` | UI behaviour (search, menu, share, ToC) |
| `scripts/generate-pages.ps1` | All HTML page templates + article data validation |
| `scripts/generate-feeds.ps1` | sitemap.xml / rss.xml / robots.txt / articles-slim.json |
| `scripts/generate-site.ps1` | Entry point, runs all scripts |
| `docs/tabi-mockup.html` | Design mockup (reference only) |
| `scripts/*-articles*.ps1`, `migrate-*.ps1` | One-off migrations, already applied. Safe to delete. |

---

## Design Spec

- **Concept**: Wabi-sabi minimal — generous whitespace, kanji accents, vermilion highlight
- **Colors**: `--ink #111111` / `--paper #f7f4ef` / `--accent #b5271f` / `--gold #c9a84c`
- **Fonts**: Noto Serif JP (headings) / Noto Sans (body)
- **Reference**: `docs/tabi-mockup.html`
- **Breakpoints**: 1024px → 768px → 480px

---

## Article Schema

See `AGENTS.md` for the full article JSON schema used in `articles.json`.

---

## Script Execution

```powershell
# Regenerate all pages (run after any change to articles.json or scripts)
powershell -ExecutionPolicy Bypass -File ".\scripts\generate-site.ps1"
```

Always regenerate before committing. Never hand-edit generated HTML.

---

## Branch Strategy

```
master            Always deployable. GitHub Pages auto-deploys on push.
claude/<task>     Claude Code working branches — PR to master
codex/<task>      Codex working branches — PR to master
```

**Never commit directly to master for feature work.**

---

## PowerShell Encoding Rule

Never write Japanese directly in `.ps1` files — causes garbling.  
Always use HTML entities.

```powershell
# Bad
$lines.Add('旅')

# Good
$lines.Add('&#26053;')
```

---

## Current Status (2026-08-06)

### Done
- [x] `site.config.json` — categories, tags, domain configured
- [x] `articles.json` — 63 articles, all with full bodies
- [x] `styles.css` — design system
- [x] `script.js` — search, mobile nav, share, ToC scroll-spy
- [x] `scripts/generate-site.ps1` — full page + feed generation
- [x] `index.html`, `articles.html` archive, category / tag / article pages
- [x] GitHub Pages deploy workflow

### Known gaps
- [ ] `googleAnalyticsId` is empty, so no analytics runs and no cookie banner is shown.
      Setting it in `site.config.json` enables both; the tag only loads after consent.
- [ ] Hero images are served at full size (~250 KB each). `srcset` would cut this.
- [ ] Google Fonts is render-blocking with three families.

---

## Local Preview

Every page has `<base href="$siteUrl/">`, so a production build cannot be previewed
locally — the browser would fetch CSS, JS and links from the live site. Build against
a local origin instead:

```powershell
$env:TABI_SITE_URL = "http://localhost:8080"
powershell -ExecutionPolicy Bypass -File ".\scripts\generate-site.ps1"
python -m http.server 8080
```

**Re-run the generator without `TABI_SITE_URL` before committing**, or the committed
HTML will point at localhost.

---

## Encoding

`articles.json` was once corrupted by a script that read it with `Get-Content -Raw`
without `-Encoding UTF8`; PowerShell 5.1 defaults to ANSI (CP932 here), which turned
every em dash and curly quote into mojibake. Always pass `-Encoding UTF8` when
reading, and write with `New-Object System.Text.UTF8Encoding($false)`.

The `.ps1` files themselves have no BOM, so **PowerShell 5.1 reads them as ANSI**.
Any non-ASCII character written literally in a script will be mangled on output —
use HTML entities or numeric character references instead (this is the existing
"never write Japanese directly in .ps1" rule, and it applies to `—` and `'` too).
